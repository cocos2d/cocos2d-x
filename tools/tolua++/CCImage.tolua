
class CCImage
{
    int getDataLen();
    bool hasAlpha();
    bool isPremultipliedAlpha();
    bool saveToFile(char *pszFilePath, bool bIsToRGB = true);

    short getWidth(void);
    short getHeight(void);
    int getBitsPerComponent(void);
};
